function gen(){
    // // Math.random(), this will gives us the random numbers between 0 and 1 in decimal floating.
    // var x = Math.random();
    // alert(x);

    // // User defined Range
    // // Math.random() * (highestNumber - lowestNumber) + lowestNumber
    // var x = Math.random() * (100 - 1) + 1;
    // console.log(x);

    // // User defined data type like int, float, double etc
    // // Math.floor(Math.random()* (highestNumber - lowestNumber)) + lowestNumber
    var x = Math.floor(Math.random() * (5000 - 1000)) + 1000;
    console.log(x);

    // Note: We can create OTP using these methods of Math in JavaScript
}